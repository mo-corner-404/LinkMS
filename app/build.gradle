apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "mo.app.linkms"
        minSdkVersion 8
        targetSdkVersion 21
        versionCode 2
        versionName "1.01"
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles 'proguard-project.cfg'
        }
    }
}
